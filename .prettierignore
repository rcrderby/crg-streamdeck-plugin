node_modules/
*.sdPlugin/bin/
*.sdPlugin/logs/
dist/
LICENSE

# A vendored library, kept byte for byte as its author published it
*.sdPlugin/ui/sdpi-components.js

# yamllint and markdownlint own these, through the Super Linter
# configuration in .github/linters
*.yml
*.yaml
*.md

# Visual Studio Code rewrites its own settings file, and the spell
# checker expands cSpell.words every time it adds a word
.vscode/settings.json
